import React from 'react';
import Base from '../core/Base';
import Navbar from '../core/components/navbar';

const UserDashBoard = () => {
    return (
        <div>
            <Navbar/>
            <Base title="This is User dashboard"></Base>
        </div>
    );
}

export default UserDashBoard;
